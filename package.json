{
  "name": "my-mega-upload-app",
  "version": "1.0.0",
  "description": "A simple app to upload files to MEGA",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5", // Add multer if not already present
    "mega": "latest" // Use "latest" if a specific version is not required
  },
  "engines": {
    "node": ">=14.x"
  }
}
